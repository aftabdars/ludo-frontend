import React from "react";
import { Subscription } from "use-subscription";
export interface SubscribableRouter extends Subscription<string> {
    navigate: (nextUrl: string) => void;
}
export declare function createBrowserRouter(basePath?: ""): SubscribableRouter;
export declare function createMemoryRouter(initialPath?: string): SubscribableRouter;
interface RouterProviderProps {
    children: React.ReactNode;
    router: SubscribableRouter;
}
export declare const RouterProvider: ({ children, router }: RouterProviderProps) => React.FunctionComponentElement<React.ProviderProps<SubscribableRouter>>;
export interface RoutesObject {
    [key: string]: (props?: any) => any;
}
export declare const useRoutes: (routes: RoutesObject) => any;
export {};
//# sourceMappingURL=routes.d.ts.map